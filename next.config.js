/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/Luka-Tennis-Player',
  trailingSlash: true,
  images: { unoptimized: true },
};

module.exports = nextConfig;

